[project]
name = "agent-life-cycle"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Abdul Raqeeb", email = "nizamaniabdurraqeeb@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "openai-agents>=0.0.6",
]

[project.scripts]
agent-life-cycle = "agent_life_cycle:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
